# ---------------------------------------------------------------------------
# Lightweight dependency set for CI test runs.
# Deliberately EXCLUDES heavy, lazily-imported libs (torch/sentence-transformers,
# chromadb, streamlit): the unit/integration/e2e tests use fakes for those, so
# CI stays fast. Install the package with `pip install -e . --no-deps` so
# pyproject's full runtime deps are not pulled in.
# ---------------------------------------------------------------------------
fastapi==0.111.0
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
SQLAlchemy==2.0.31
alembic==1.13.2
httpx==0.27.0
structlog==24.2.0
pypdf==4.2.0
# (langgraph intentionally omitted: orchestrator test importorskips it)

# test tooling
pytest==8.2.2
pytest-cov==5.0.0
pytest-asyncio==0.23.7
